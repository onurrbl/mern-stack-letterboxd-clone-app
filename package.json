{
  "name": "mern-stack-kinda-letterboxd-app",
  "private": true,
  "scripts": {
    "install-all": "cd client && npm install && cd ../server && npm install",
    "client:start": "cd client && npm run start",
    "client:build": "cd client && npm run build",
    "server:dev": "cd server && npm run dev"
  }
}
